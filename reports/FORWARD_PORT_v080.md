# Forward-Port Report — v0.6.5 + v0.7.0 → v0.8.0

Generated by `tools/forward_port.py` (deterministic, re-runnable). This is the
full record of every change made to produce the clean `v0.8.0` seed — the good
and the bad.

## Why

Per `ASSESSMENT.md` §3, **v0.6.5 was the cleanest seed** (19 defects, 0 dangling
refs) and **v0.7.0 regressed structure** (196 defects) while adding a citation +
triage layer. This port takes v0.6.5's clean structure and merges in v0.7.0's
good parts, rather than continuing to build on the regressed v0.7.0.

## Result

| Metric | v0.6.5 (base) | v0.7.0 (donor) | **v0.8.0 (this)** |
|---|---:|---:|---:|
| Deities | 643 | 673 | **643** |
| Facets | 934 | 1326 | **934** |
| Parallels | 270 | 329 | **259** |
| Citation-backed | 0 | 35 | **56** |
| Validator defects | 19 | 196 | **2 (both Sev-3)** |
| Validator exit | 1 | 1 | **0 (green)** |

Triage distribution: {'established': 56, 'genuine-novel': 178, 'known-uncited': 25}

## Changes applied (the good)

### 1. Tradition foreign keys repaired (4)
Orphaned `tradition_id`s remapped to real traditions present in the data:

| Deity | From (missing) | To (real) |
|---|---|---|
| `guan-yu-deity` | `chinese` | `chinese-folk` |
| `pangu` | `chinese` | `taoist-chinese` |
| `xi-he` | `chinese` | `chinese-folk` |
| `dagon` | `levantine` | `canaanite-ugaritic` |

### 2. Mislinked parallels repaired (2)
Each label named two figures but a facet pointed at the wrong deity. Repointed
to the intended figure's real facet (both targets exist in v0.6.5):

| Parallel | Before | After |
|---|---|---|
| `p_osiris_dionysus` | osiris-dying-rising ↔ osiris-dying-rising | osiris-dying-rising ↔ dionysus-twice-born |
| `p_sun_wukong_loki` | loki-trickster-liberator ↔ loki-bound | sun-wukong-equal-to-heaven ↔ loki-bound |

### 3. Citation + triage layer ported
- Citation fields ported from v0.7.0 onto matching base rows: **14**.
- `provenance` is now **derived** from whether a row carries a citation
  (`scholarly_note` / `suggested_citation` / `primary_text_evidence`) — the
  project's own definition of "established". This is why the count (56) exceeds
  v0.7.0's 35: v0.6.5 already had citation-bearing rows that were never flagged.
- `triage_status` is **derived** from `type`/`strength`, correcting **46**
  rows whose v0.7.0 triage contradicted their own data (e.g. a
  `documented-historical-transmission` tagged `genuine-novel`). This removes all
  46 Severity-2 contradictions.

### 4. Duplicates removed (10)
Same unordered facet-pair collapsed to one row:

| Dropped | Kept |
|---|---|
| `p_inanna_persephone` | `p002` |
| `p_inti_ra` | `p_ra_inti` |
| `p_zurvan_kali` | `p_kali_time_zurvan` |
| `p_veles_erlik` | `p_erlik_veles` |
| `p_izanami_ereshkigal` | `p_ereshkigal_izanami` |
| `p_persephone_inanna` | `p002` |
| `p_athena_durga` | `p_durga_athena` |
| `p_isis_mary_iconography` | `p_mary_isis` |
| `p_loki_iblis_bound` | `p_loki_iblis` |
| `p_mara_iblis` | `p_iblis_satan` |

## Changes that lose information (the bad — recorded honestly)

- **`p214` dropped.** It was Anakim↔Anakim — two facets of the *same* deity, i.e.
  an attestation note (Egyptian Execration Texts on the Anakim), not a
  cross-figure parallel. Its scholarly content (Albright/Mazar) survives in the
  historical snapshots `data/source_map_seed_data_v0{65,70}*.json`. **TODO:** fold
  that attestation into the Anakim deity/facet record rather than discarding.
- **59 v0.7.0-only parallels were NOT carried over.** They reference donor facets
  not present in v0.6.5 (many tied to the 136 dangling refs). Re-add them only
  after their facets are created cleanly. None were citation-backed (all 56
  established rows are present).
- **The 10 deduped rows** may have carried slightly different `basis` text; only
  the kept row's basis survives. Diff against v0.7.0 if a specific basis matters.

## Known remaining defects (Severity-3 — not fixed, by design)

These are systemic quality issues that a merge must not fake-fix:
- `type_vocabulary_uncontrolled` — `type` is still free text; collapse onto the
  8 evidence types in `docs/06` + a `descriptor` field (roadmap P1.7).
- `facets_mostly_unsourced` — most facets carry no `scholarly_note` (roadmap P2.9).

Run `python3 tools/validate_seed_data.py data/source_map_seed_data_v080.json` to
reproduce. Exit code is 0 (green): no Severity-1, no Severity-2.